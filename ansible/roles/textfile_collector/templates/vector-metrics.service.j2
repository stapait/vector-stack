# Gerado pelo Ansible (vector-provision.yml) — não editar manualmente.
[Unit]
Description=Gera metricas custom (.prom) de tamanho/ultimo log por app

[Service]
Type=oneshot
User={{ vector_system_user }}
Group={{ vector_system_group }}
ExecStart=/usr/local/bin/log-metrics.sh
