---
- name: Provisionar concentrador de logs (Vector)
  hosts: vector
  become: true
  gather_facts: true
  roles:
    - os_tuning
    - common
    - vector
    - node_exporter
    - textfile_collector
    - logrotate
    - archive_logs
